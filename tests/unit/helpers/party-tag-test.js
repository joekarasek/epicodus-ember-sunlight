import { partyTag } from 'legislator-lookup/helpers/party-tag';
import { module, test } from 'qunit';

module('Unit | Helper | party tag');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = partyTag([42]);
  assert.ok(result);
});
